# Evolution cost (E)

For each change tournament record kernel edits, existing-data rewrites, migrations, new author-facing concepts, surface edits, adapter edits, and generated-artifact changes.

Expected low-core-edit changes include new predicates/concepts, policies, protocols, identity mechanisms, visualizations, organizations, and storage targets. Evidence, not intent, decides.

