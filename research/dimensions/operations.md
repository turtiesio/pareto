# Operational complexity (O)

Count runtime services, workers, queues, reconcilers, deployable units, durable process states, recovery paths, and distinct failure modes. Record which parts protect semantics and which only improve availability or speed.

Recovery should prefer durable evidence and deterministic reconstruction over hidden process memory.

