# Portability/realization cost (P)

For each semantic requirement and target classify the realization as exact, transactionally enforced, runtime-enforced, eventually reconciled, not safely realizable, or unsupported. These are reporting terms, not presumed kernel primitives.

Never weaken the semantic contract to make an adapter appear portable. Record target-specific indexes, transactions, guards, reconcilers, and failure behavior.

