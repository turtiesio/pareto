# Trusted computing base (T)

Count and identify code/configuration that must be correct for each claimed guarantee: decoder, verifier, evaluator, compiler, generated guard, adapter, authorization boundary, bootstrap keys, and canonical encoder.

Lines of code are a crude proxy. Also record semantic surface, privilege, replaceability, test-vector coverage, and whether two independent implementations agree.

