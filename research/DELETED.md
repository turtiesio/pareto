# Deletion log

Nothing has yet earned deletion. This file records removals only after replaying the relevant corpus.

Candidates already scheduled for deletion attempts:

- media type as a mandatory property of opaque evidence;
- globally unique identifier as opposed to a stable enclosing record address;
- parsed map/list/value tree as the authoritative preservation form;
- capture timestamp when the acquisition system has no trustworthy clock reading;
- “valid/invalid” as a property of uninterpreted bytes.

