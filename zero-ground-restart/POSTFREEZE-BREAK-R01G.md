Verified SHA-256: `9fcf79f7544e3fe7d11f0830e224635758921f199e4478bb4180ada991629008`. Read-only audit; no other repository artifact was inspected and no file was modified.

## Post-freeze verdict

| Area | Classification | Finding |
|---|---|---|
| Eleven-message alphabet | PASS | Exactly ten one-byte messages plus `30 00`; every message has a total fault-free transition/output in every reachable reduction. |
| Message framing | PASS | `C!30 00 ≺ R!e0 02` is distinct from `C!30 ≺ R!e0 03 ≺ C!00 ≺ R!80 00`, even before comparing replies. |
| Half-close/STOP | PASS, gated | Clean `C↓ ≺ R!87` is structurally distinct from every nonempty `C!x` and is terminal. Crashes around `C↓` are not defined by the separate STOP suite. If “crash before or after any crossing” includes `C↓`, then `C↓ ≺ K!CRASH ≺ K!RESTART` is underspecified. |
| “Empty submission” | contradiction | The four displayed suite histories are STOP, `fe`, `30 00`, and `30` followed by `00`. There is no empty submission. Empty datagrams are explicitly unsupported; if “empty” means `M^0`, its history is `ε` and was omitted. There is no legal common continuation with completed STOP because STOP halts. |
| Malformed count wording | contradiction | `C!fe ≺ R!e0 01` is defined as unknown opcode, while only `C!30 00 ≺ R!e0 02` is the malformed supported sample. Calling both “the two malformed messages” contradicts the rejection taxonomy, though the two rejected sample bytes are total. |
| Continuing crash grammar | PASS | Atomic crossings, sequential client behavior, old/new mutation resolution, read-only/rejection discard, all three action projections, quiescent crash, restart, and failure-free recovery cover the legal continuing product. Second crash, recovery crash, and input before `R!88` are expressly gated. |
| Old/new mutation recovery | PASS | State-changing `00/01/10/11/20/50` has precisely old/new quotient outcomes; acknowledged replies imply new durability. Semantic no-ops have old=new publicly. |
| Read-only/rejection recovery | PASS | Example: after `C!10 ≺ R!81 10`, `C!30 ≺ K!CRASH ≺ K!RESTART ≺ R!88 ≺ C!30` ends in `R!e0 04`, with no delayed reply from the interrupted query. |
| Action/retry | PASS | Prepared history `C!10,R!81 10,C!00,R!80 00`; then `C!40,D!a0 00,K!CRASH,K!RESTART,R!88,C!40,D!a0 00,R!84` correctly exposes two attempts for two occurrences. Before-`D` crash gives none; after-`D` crash gives one and no automatic replay. Receiver effect remains gated. |
| Public versus physical history | underspecification | Byte-old/byte-new is not a declared boundary label. From `H=[]`, the same public future `C!10 ≺ K!CRASH ≺ K!RESTART ≺ R!88 ≺ C!30` permits `R!e0 03` or `R!e0 04`. The in-flight row therefore compares instrumented outcomes, not two exact public histories. |
| Nondeterministic equivalence | underspecification | Section 5 never states may-trace set equality. Pointwise universal reading makes even `H ~ H` fail on the preceding future. Set equality makes the fourteen-class result coherent and matches §8, but is only explicit for L/S comparison. |
| 133/table counts | PASS | Independent reduction gives the exact fourteen rows and counts shown; revision totals are `111/22`. |
| Section 11 multiset | false prediction | Correct multiset is `{45,15,15,14,14,14,2,2,2,2,2,2,2,2}`. Printed multiset has four `14`s and seven `2`s and sums to `145`, contradicting both the table and `133`. |
| Three discriminator probes | PASS | `60`, `00,30`, and `10,30` are injective over `(r,t,o)` and separate all eighteen classes within two client messages. |
| Five survival witnesses | PASS as witnesses | All five produce their stated differing bytes. |
| Claimed witness minimality | false prediction | Two lose the declared message-order tie-break. For revision, `H=ε`, `H′=C!50≺R!85 01`, common future `C!50` gives `R!85 01 / R!e0 06`; `50` precedes listed `60`. For interpreter absence, `H=ε`, `H′=C!10≺R!81 10`, future `C!20` gives `R!e0 05 / R!82`; `20` precedes listed `30`. The other three listed futures are shortest for their fixed isolated pairs. |
| Remaining minimization order | underspecification | No ordering is given for pair orientation, distribution of messages among `H,H′,F`, crash position/ordinal within a crash category, or equal-cost words beyond “declared message order.” Thus it does not define a unique all-pair representative. |
| Behavioral forget claims | PASS, conditional | Given set semantics, equal `(r,t,o)` implies equal future trace sets; multiplicity, overwritten values, rejection/query/action occurrences, and prior replies have no future effect. Successful query/action forgetting is stipulated by rule 4.9 but is not witnessed in the 133 pre-cut corpus because neither can succeed within two messages. |
| Physical absence/fresh generation | unsupported/gated | Exact histories prove independence, not absence of unused registers or caching. `C!fe≺R!e0 01`, followed by `C!60`, yields `R!86 03 00 40 a0` whether or not `e0 01` was saved. Repeated `60` likewise cannot distinguish recomputation from a correct cache. |
| Encodings | PASS | Packed `c`, length-plus-three-slot representative, and `[r,u,v]` are each injective on all eighteen Cartesian states; every representative replays validly. |
| L capacity/replay | PASS at logical-model level | Five messages can cause at most five successful mutator appends. `ff` cannot collide with a valid opcode, old crash leaves a contiguous shorter prefix, and new crash appends an oracle-valid successful opcode. The `9,331` raw-image bound is correct. |
| S reachability | PASS at logical-model level | All eighteen values `00..11` are reachable; every issued state-changing write has valid old/new endpoints. No-op requests issue no S write. |
| One-byte storage law | unsupported/gated | It is a clear axiom, not evidence about any physical adapter. Atomic old/new byte behavior, durability ordering, overwrite safety, and real byte cost remain implementation obligations. |
| L/S fault-set correspondence | PASS logically; UNKNOWN empirically | By induction on a request: changing mutators yield the same old/new quotient set, no-op L images collapse to S’s singleton public state, and nonmutators are identical. Independent code, actual power faults, and exhaustive equality remain unexecuted. |
| Schedule count | underspecification / false if treated as exact | `17` is only a loose upper bound formed from incompatible maxima, not a nonduplicate per-script count. Prepared cut `[10,00]` plus `[40,40,40]` has ten gaps and zero writes; `[]` plus `[00,01,10]` has seven gaps and three writes. Under §7 canonicalization, count is `1+g+w`, maximum `11`; summing all scripts gives L `1,790,701`, S `1,748,699`, total `3,539,400`. Literal “all gaps plus both write outcomes” double-counts write-bearing gaps and gives total `4,026,400`. Neither makes `6,620,208` a count of nonduplicate bounded schedules. |
| Other Section 11 counts | PASS, conditional | `11`, `2` rejected samples, `133`, `14`, `18`, `1,464`, `194,712`, max three post-cut writes, max ten gaps, `9,331`, eighteen S images, five/one logical bytes, and `1,368 = C(19,2)·4·2` are arithmetically sound under their stated interpretations. |
| Exact-history restriction theorem | unsupported/gated | No legal byte in `M` encodes the proposed characteristic interpreter, and its history-snapshot time is unspecified. Under query-time “prior history,” authoring has already changed the history, so the displayed proof does not follow literally. It is a conditional result about a different interpreter language, not an executable seed theorem. |
| TCB boundary | PASS logically; UNKNOWN empirically | The `D` boundary consistently excludes receiver semantic effect while retaining the attempt adapter inside the TCB. Parser, framing, reduction, byte adapter, recovery, ordering, identity, harness, and oracle are listed; their independent attacks remain unperformed. |
| Claimed capabilities | mixed | Replacement, retirement, querying, functional interpretation, attempt crossing, coded rejection distinctions, revision/identity, and abstract crash/recovery are genuinely trace-visible. `00/01` is only a submitted-bit register from histories; `10/11` is observationally indistinguishable from selecting two frozen built-ins rather than authoring tables. Physical power loss and two independently authored unlike realizations are UNKNOWN pending implementation. |

The frozen seed therefore has a correct deterministic quotient core and coherent logical L/S candidates, but it is not internally clean: the class multiset and two minimality predictions are false, the empty-submission claim is unsupported by its own histories, and crash equivalence lacks the set-valued semantics needed to remain an equivalence.
