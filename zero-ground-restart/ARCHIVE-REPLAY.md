# Quarantined archive replay

The frozen candidate and breaker hashes were recorded before this replay.
`../ground.md` has SHA-256
`c1f1171fbca0ed616255e5f389a7a9127d93eddeb0063c9d2fe09e45f1766278`.
It is the 155-line restart charter itself, not an older solution corpus, and
contains no prior ontology or concrete historical collision pair.

Its requirements were replayed only as attacks:

| ID | Executable attack/acceptance condition | B0 disposition |
|---|---|---|
| A01 | reset/swap every undeclared clock, service, secret, prompt, deployment, organization, and human memory | boundary failure or `UNKNOWN`, never evidence for merge |
| A02 | vary every permitted continuation, viewer, interpretation, policy, action, and realization, including compositions | claims remain scoped to the frozen context grammar |
| A03 | compare contract-required observations using an independent oracle, not candidate behavior | hard conformance gate |
| A04 | find equal encodings with unequal residual signatures after every simplification | collision gate |
| A05 | also remove over-preservation until encoding kernel equals the bounded quotient | exactness gate |
| A06 | minimize under the frozen well-founded order | otherwise “smallest” is `UNKNOWN` |
| A07 | evaluate every surviving candidate on every total-system dimension in the same round | Pareto relation only; no scalar |
| A08 | erase each candidate-addressable persisted fragment and cold boot | retain only with bounded witness |
| A09 | disable compiler, verifier, selector, recovery, invalidation, explanation, migration, and trust mechanisms too | mechanism/TCB ledger |
| A10 | attempt every quotient-class merge | each rejection needs an oracle witness |
| A11 | rebuild twice from only MUST information plus named, available specification | determinism/availability gate |
| A12 | remove services, prompts, ambient settings, operator memory, and conventions after deletion | charge all externalized state |
| A13 | run identical traces, failures, migrations, and observation comparator on unlike implementations | descriptions alone remain unsupported |
| A14 | measure blind-human recall, discovery, entry, verification, time, and errors | cognition remains `UNKNOWN` without protocol/data |
| A15 | mutate every correctness-critical code/config dependency | locate moved TCB |
| A16 | strip semantic category assumptions from each witnessed distinction | verdicts do not create primitive types |
| A17 | delete each named abstraction while retaining its distinction by another encoding | name/mechanism survives only on its own witness |
| A18 | delete dedicated authoring constructors independently | a distinction does not entail a constructor |
| A19 | treat passes only as failure to falsify and retain breaker/oracle independence | no implementation-to-architecture inference |
| A20 | demonstrate omitted capabilities are unnecessary or mark unsupported/cost replacements | no zero-cost absence |
| A21 | hash public artifacts, then run fresh-domain and hidden permitted contexts | pass never broadens scope |
| A22 | reject search/navigation infrastructure as a substitute for the first experiment | scope gate |
| A23 | repeat to the declared transform fixed point and state its grammar limit | no global irreducibility overclaim |
| A24 | require survival/rebuild/forget verdicts and minimized witnesses, not a kernel | milestone acceptance gate |

No extra archived counterexample was available. Fresh hidden attacks therefore
remain independently necessary.
