# ZERO GROUND restart

`FROZEN-B0.md` and `BREAKER-B0.md` were independently derived and frozen before
the quarantined prior archive (`../ground.md`) was read. The archive may now
contribute attacks only; it cannot supply candidate structures or names.

Current evidence artifacts:

- an executable finite history/context enumerator and independent raw-trace
  oracle;
- quotient and right-congruence computation;
- union-domain proof enumeration (`enabled`/`disabled` are domain markers, not
  unrecorded response frames);
- candidate public `persist`/`recover` differential tests in B2, plus a
  separate B3 fresh-process producer/consumer reconstruction probe;
- exhaustive declared deletion/merge search with minimized witnesses;
- fresh-domain, hidden-breaker, and archived-attack results;
- simultaneous non-scalar candidate measurements and explicit unknowns.

`EVIDENCE-B2.json` is the deterministic executed B2 report; its encode/recover
checks are explicitly in-process. `PROCESS-RESTORE-B3.json` separately records
producer-exit reconstruction in two fresh consumer processes per candidate over
all 82,584 classes and 1,403,928 one-step operations per consumer. B3 does not
establish crash/torn-write durability, unlike-runtime portability, or unlike
physical realization.

The consolidated result is in `FIRST-MILESTONE.md`; `FORMAL-AUDIT-B2.md`
records the independent proof audit and the exact scope of its claims.

Post-milestone realization attacks are deliberately separate from B1. The
pre-implementation finite contract is `REALIZATION-CONTRACT-R0.md`; guest-level
ext4/tmpfs execution is recorded in `REALIZATION-EVIDENCE-R0.json`; the physical
claim gate is `REALIZATION-GATE-R0.md`. `rust-realizer/` independently
reimplements the frozen B1 language contract in Rust and exchanges both full
encodings bidirectionally with CPython. `RUNTIME-BREAKER-R0.json` is the
precommitted held-out attack, `TRANSITION-WIRE-R1.md` records a late byte-format
dependency discovered by that implementation, and `RUNTIME-EVIDENCE-R1.json`
records the cross-runtime run.

`REALIZATION-AUDIT-R1.md` is authoritative for interpreting those newer
results. In particular, it preserves rather than hides three falsifications:
the in-band R0 suite digest is redundant, mechanism-deletion stage markers are
not truthful under the literal contract, and the stronger portable diagnostic
taxonomy fails 21 of 140 checks even though all frozen B1 semantic outcomes
match. Unlike physical realization and human cognition remain unknown.
