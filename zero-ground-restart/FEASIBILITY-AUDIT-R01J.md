# ZERO GROUND R0.1J — Feasibility audit

## 0. Decision and claim boundary

**FIRST MILESTONE: FAIL / NOT ACHIEVED.**

R0.1J has a coherent bounded **formal** continuation quotient under the fresh
blind and literal audits, but it does not establish the smallest total system
required by the ZERO GROUND contract. Those audits separately support the
central counts—153 PUBLIC outcome functions and 329 contractual all-scope
classes—and the executable derives the same numbers under a narrower fixed-word
probe basis. The executable also closes finite controller and scheduler key
domains from normalized seeds. None of that cures the five fresh finite defect
families. The executable reports five failing checks across the
persistence/evidence families because it splits the envelope defect into two
facets; fresh F01 raw-router totality remains a separate static FAIL that the
executable did not directly test. Nor do the formal results supply the missing
physical, externalization, cognition, portability, or TCB experiments.

The decisive boundary is:

- the fresh blind/literal analysis supports the finite FORMAL quotient, while
  the executable's fixed-word refinement does not prove its adaptive probe
  basis complete;
- the declared total evidence and raw-operation contract does not;
- the frozen Section 10 persistence ledger is incomplete even for that formal
  quotient; and
- no physical representation or unlike realization is admitted or tested.

A passing formal model is a falsification result about bounded histories. It
is not an architecture, storage design, persistence proof, implementation, or
physical recovery result. This audit selects none of those.

## 1. Frozen artifacts and quarantine chronology

### 1.1 Commit and byte ledger

| Artifact | Commit | SHA-256 | Role |
|---|---|---|---|
| `HISTORY-SEED-R01J.md` | `c1ddc239ec17516e53c30d8225a396c3348dbdde` | `7c9b2a742eab81f6c104b6ca3566315ddb29ecc63d216d25cff91bf1c70836dc` | Frozen candidate authority |
| `BLIND-ATTACK-PACK-R01I.md` | `7f60816df97bed16bcfc80f837528725e1efa4b8` | `0b5a7c3f91af525559570be8e77a69a83adc9b980b97b0727877caca1974d24b` | Ontology-independent attack vocabulary |
| `POSTFREEZE-BREAK-R01J.md` | `24cb91056dbcc36b021c8c1e19ccd1e7477e0273` | `ed8675d7b2f3d53b8e35321e2361a0480665ce36e498d8ed5f8e3b15bac96d8a` | Fresh blind break and independent recount |
| `LITERAL-SPEC-AUDIT-R01J.md` | `bf0a237870a2abfc4f34585a44049091ae5510cb` | `370642e1ea148c4702a373a3e33347d3987c160db1fe41dc5e9364cb4ead3956` | Independent literal/canonical audit |
| `ARCHIVE-REPLAY-R01J.md` | `41a0201a90ca23baad93a7847d732781c2159b15` | `94a07058e89b3c34afb4fed6147f463837f1f3483248b80f40e93843e05bbf0d` | Post-break replay of quarantined attacks |
| `r01j_history_experiment.py` | `1db2506930b58d9178a31d13fbe2d0774b7c08c0` | `55ae07d48029e326cfe32390e8cd5ad4254a6624902a7752a561cc2881120d35` | Fail-closed executable falsifier |
| `EXPERIMENT-RESULT-R01J.md` | `f7e8c4d666b2ab4e61a72739bf92d7c036bd8bb3` | `67a42e86a1b997510de88f1b95d48c8aa10235c57300016a54f9c099bac932fc` | Corrected author and independent run-scope record |
| `PERSISTENCE-COLLISION-LEDGER-R01J.md` | `f503e80daef498902e74987115adb7825ba270b4` | `ce648de3379817ace52864c87ca1d9427a6bc01bdb717603fba0510cbd38eb3c` | Audit-only corrected responsibility ledger |

The final executable commit changes only `r01j_history_experiment.py`. The
candidate digest is checked before semantic work; any finite FAIL makes the
process exit nonzero.

### 1.2 Quarantine sequence

The chronology that is evidenced by the files is:

1. the candidate bytes were frozen at `c1ddc239...`;
2. the fresh break and literal audit read the frozen candidate plus the blind
   pack, and expressly did not read the R0.1I candidate, archive, implementation,
   proposed repair, or prior solution;
3. the fresh break was frozen at `24cb910...` before archive replay;
4. only then were R0.1H/R0.1I reports admitted, solely as counterexample
   shapes reconstructed in R0.1J's own alphabet and relation;
5. the executable was subsequently used as a falsifier; and
6. the corrected persistence ledger was written as an audit verdict, not a
   repair to the candidate.

The archive added no new FAIL and no smaller counterexample. It corroborated
the already-frozen router and Q7 minima and located the complexity moved by
earlier repairs. This sequence establishes breaker/archive quarantine. These
artifacts do not independently measure the candidate author's memory or
cognitive independence, so no stronger provenance claim is credited.

## 2. What the formal experiment did establish

### 2.1 Direct finite results

The author's post-commit run exited `1` with `63 PASS`, `5 FAIL`, and `4
UNKNOWN` evaluations. Its complete stdout had SHA-256
`fa132536f0aff45e2592f23cccd04c88cd7e50bfb51cd409b4a89a705b12a930`;
the JSON recorded 287.949434 seconds of whole-program elapsed time, while the wrapper
measured 288.049929 seconds and 175,232 KiB maximum resident set.

An independent run also exited `1`, took 294.08 seconds wall time and 292.63
seconds user CPU, reached 174,052 KiB maximum resident set, and emitted stdout
SHA-256 `7d7aff5aff0e9ce04a1ac7ebcc316361428efca2ff24d2b21fa80e7f1a558588`.
The JSON contains run-specific timing, so byte-identical output is not
expected. The unequal hashes alone do not prove timing is their only difference
and do not independently reproduce the named evaluations. The author's full
output supplies the `63/5/4` summary and verdict names; the separate run verifies
the frozen source's exit behavior and resource envelope and supplies an
independent whole-output digest. No runtime-stripped normalized comparison was
performed. These are falsifier costs, not service latency, recovery time,
storage cost, or operational bounds.

The executable directly reproduced:

| Finite domain/result | Exact value |
|---|---:|
| Clean histories | 157 |
| Clean residual classes | 14 |
| Clean same / unequal / total unordered pairs | 2,351 / 9,895 / 12,246 |
| Exact recovery histories generated and folded | 1,208,272 |
| All declared cuts | 1,208,429 |
| Normalized recovery conditions | 854 |
| Fixed-word recovery PUBLIC / contractual partition | 139 / 315 |
| Derived combined PUBLIC image / contractual class count | 153 / 329 |
| Decision-key closure `D` | 82,224 |
| Gap-key closure `G` | 64,067 |

The script derives 153/329 by adding the fourteen clean residual classes to
the fixed-word 139/315 recovery partitions and applying one common FIN/pass
clean-versus-recovery no-merger witness. It does not independently partition
the full adaptive controller-table relation.

The literal audit further decomposes `D` as
`d=0:78,091`, `d=1:3,977`, `d=2:153`, and `d=3:3`. Thus 4,133 controller
entries are free, 78,091 are forced FIN, a complete controller is 82,245
bytes, and there are `13^4133` legal controllers. `G` includes 12,929 END
keys; the scheduler vector is 8,009 bytes, its complete encoding is 8,030
bytes, and there are `2^64067` schedulers. These are formal policy-domain
costs, not persisted-service-state requirements. The executable's D/G
exploration is seeded from fourteen clean residual coordinates plus the 854
normalized recovery conditions, not from every exact prefix as a distinct
seed.

### 2.2 Direct, symbolic, and unexecuted boundaries

The 1,208,272 exact recovery prefixes were directly generated, folded, and
serialized. Arithmetic covered all 12,246 unordered clean-history pairs;
separator execution covered all 91 unequal residual-value pairs. The quotient
refinement itself applies each of 1,885 fixed failure-free words of length zero
through three to each of the 854 normalized recovery conditions, using the same
word on both selector branches. It does not enumerate the adaptive controller
table universe and does not prove that this fixed-word probe basis is complete
for it. Exact cuts are then lifted through the normalized condition derived
from each prefix. That exact-history lift is symbolic; it is not 1,208,272
independent future evaluations.

The blind break gives formal exact-history class-size histograms by lifting
the exact condition frequencies. The final executable did not independently
emit or exhaustively validate the full exact-history multiplicity corpus, so
that stronger executable claim remains UNKNOWN. Its Q1 search minimizes only
within eight FIN-path scheduler masks and eligible scopes. Q2 is constructed
and checked for separation and codec behavior but is not globally minimized;
neither control establishes arbitrary-pair global minimization.

No run performed a physical deletion, cold reconstruction, process/power-loss
recovery, external-carrier severing, receiver-effect observation, controlled
human study, cross-platform comparison, one-at-a-time TCB perturbation, or
unlike-physical-realization comparison.

### 2.3 Combined formal support that survived

The fresh blind and literal audits provide the stronger analytic support for
the complete FORMAL relation. The executable provides narrower fixed-word,
closure, and control evidence. With those sources kept distinct, the following
bounded facts survive:

- one ungated relation compares clean and every recovery phase;
- the clean class multiset is
  `59,17,17,16,16,16,2,2,2,2,2,2,2,2`;
- the recovery contractual histogram is
  `263x1,9x8,27x9,14x10,2x68`;
- the combined normalized contractual histogram is
  `277x1,9x8,27x9,14x10,2x68`;
- no clean class joins a recovery class under the common FIN/pass future;
- unequal clean residuals have zero-ordinary-C SELECTOR/PRIV separators and
  one-ordinary-C PUBLIC X separators;
- OLD/NEW application, T pre-A/post-A capture counts, FIN-pending STOPPED, and
  terminal no-STOPPED behavior hold formally;
- prefix-only `EncCut`, formal/raw carrier separation for completed finite
  observations, and viewer admissibility hold in the formal model;
- the literal audit proves Section 8.7 witness existence and uniqueness from
  the finite policy domains, while exact arbitrary-pair bytes remain UNKNOWN;
  and
- the literal audit and executable both obtain the exact D/G closure sizes,
  though neither result is a physical persistence claim.

The analytic audits support the bounded formal quotient; the executable's
matching numbers are not, by themselves, a completeness proof for adaptive
policies. None of these results establishes the candidate's larger
total-system contract.

## 3. Finite failure boundary

### 3.1 The five executable FAILs

| Executable evaluation | Minimized collision or contradiction | Consequence |
|---|---|---|
| `responsibility_table_includes_recovery_closure_owed` | Clean `H()` versus its immediate DOWN-idle recovery cut have equal `r=(U,EMPTY,E0),d=3`; FIN/pass yields PUBLIC `FIN,STOPPED` versus `READY,FIN,STOPPED`, while SELECTOR is empty versus RESUME/ALLOWANCE. | Section 10's exclusive S1-S5 ledger omits a contract-forced phase responsibility. |
| `timeout_progress_evidence_is_collision_free` | Timeout before any crossing and timeout after captured typed FIN both encode as kind 1 with an empty body. | Deleting the captured prefix merges distinguishable evidence histories. |
| `finite_conformance_failure_survives_mixed_timeout_manifest` | A two-case manifest contains one finite origin-1 mismatch and one timeout; classifier precedence returns `UNKNOWN(OBSERVATION_BOUND)` instead of `FAIL(CONFORMANCE)`. | UNKNOWN masks an independent finite FAIL, violating fail dominance. |
| `two_timeout_cases_have_per_case_envelope_binding_and_tie_rule` | Two distinct timeout cases are externally qualified differently, but there is no per-case envelope binding and no tie rule between `FAIL(NON_TOTAL)` and `UNKNOWN(OBSERVATION_BOUND)`. | The multi-case verdict is not a total function of frozen inputs. |
| `same_manifest_has_context_independent_total_verdict` | Identical one-timeout manifest bytes yield UNKNOWN with no bound proof and FAIL(NON_TOTAL) with an unbound external flag. | Manifest identity does not determine its advertised top-level classification. |

The first pair uses zero pre-cut and zero post-cut ordinary C and is the
smallest Q7 phase collision. The timeout collision differs by one captured
crossing, the smallest nonempty partial observation. A mixed-manifest masking
witness necessarily has two cases; the context collision needs one timeout
case and two external contexts.

The missing Q7 responsibility does **not** invalidate the 153/329 formal
counts. The relation already distinguishes the clean and recovery classes; the
failure is that Section 10's allegedly complete persistence classification
does not name enough information to preserve a distinction already present in
that quotient.

### 3.2 The four executable UNKNOWNs

| Executable evaluation | Why it remains UNKNOWN |
|---|---|
| `arbitrary_pair_canonical_witness_search` | Q1 was minimized only within eight FIN-path masks and eligible scopes; Q2 was constructed and checked, not globally minimized; no arbitrary-pair search ran. |
| `zero_case_manifest_evidentiary_adequacy` | The exact 57-byte empty manifest is structurally accepted, but no generator, verifier, realization, or proposition is thereby evidenced. |
| `origin0_only_manifest_is_not_a_verifier_test` | Matching generated FORMAL traces show generator self-consistency only; no negative parser or independent realization was tested. |
| `experiment_envelope_encoding_and_manifest_binding` | No envelope bytes, signature grammar, authority, identity, or manifest-binding rule is frozen. |

These are not silent passes. The broader physical realization, portability,
human, external availability, and TCB questions remain UNKNOWN outside the
four explicit executable rows as well.

The five executable rows are not a one-for-one execution of fresh-break
F01-F05. Executable checks four and five are two facets of fresh F04's envelope
binding problem, while fresh F01's raw/router totality counterexample is not an
executable FAIL row. It remains a finite literal-contract FAIL on the blind and
literal evidence below.

### 3.3 Static contract failure not discharged by the run

Fresh break F01 remains a separate FAIL. The encoded `Eval(H,Ctl,Sch)` core
produces branches for seven representative derived start phases under one
FIN/no-crash policy pair, but that check neither covers every reachable router
configuration nor the added raw phase-action boundary:

- at the smallest pending selector barrier, raw “force SELECT old” is neither
  an encoded policy evaluation nor an outside-router refusal;
- the literal router table omits the reachable active state after FIN crossed
  and before STOPPED while the crash budget remains;
- it omits later active-idle operation after post-recovery completion when the
  crash budget is zero; and
- proposal disposal across an initial pre-action crash is reconstructed only
  by combining sections, not by the claimed total router rule.

The literal audit also notes that “raw operational descriptions” have refusal
precedence but no exact byte grammar, framing, parser, or equality rule. A
formal start-phase PASS cannot convert these raw-interface defects into PASS.

### 3.4 Other literal underdefinitions

The evidence tie and envelope defects above are finite failures. Two additional
semantic limits remain important:

- Section 10.1's “smallest” order does not define family aggregation or prefix
  distance, so its exact-minimality claims are underdefined; and
- `SUPPORTED_EVIDENCE` on a zero-case manifest has no bound proposition and
  cannot be inflated into conformance.

The corrected ledger therefore uses an explicitly marked audit-only order A1.
That order makes findings reproducible for this audit; it does not repair the
frozen candidate.

## 4. Corrected persistence classification

The following verdicts classify information responsibilities, not fields,
records, state objects, or media. A behavioral distinction may be represented
directly or derived from other surviving information. The physical location
and survival mechanism remain UNKNOWN.

### 4.1 MUST SURVIVE — behavioral continuation

Enough information must remain available to preserve all of these distinctions:

| Responsibility | Smallest or isolating witness | Future-observable difference |
|---|---|---|
| Active `O` | Idle recovery from `H()` versus matching idle recovery from `H(O0)`, both d=3 | First RESUME says `O=U` versus `O=0`. |
| Active `P` | Same construction with `H()` versus `H(AI)` | First RESUME says `P=EMPTY` versus `P=ID`. |
| Active `G` | Same construction with `H()` versus `H(E)` | First RESUME says `E=E0` versus `E=E1`. |
| Live allowance `d` | `J_I` at d=3 versus `J_I2` after completed no-op RI at d=2, with equal residual | ALLOWANCE differs before any post-cut ordinary C. |
| Clean active versus every DOWN family | A1-minimal `J_C/J_I`; direct variants `J_C/J_P`, `J_C/J_F`, `J_C/J_T` | READY and privileged recovery frames are owed only after DOWN; later FIN/STOPPED shapes differ. |
| DOWN idle NONE versus pending OLD/NEW | `J_I2/J_P`, equal r,d | SELECTOR/PRIV sees one NONE suffix versus two labeled branches even when PUBLIC agrees. |
| Directed pre/post association | Interrupted AI EMPTY→ID versus interrupted D ID→EMPTY at equal d | PUBLIC endpoint sets agree; SELECTOR/PRIV reverses OLD/NEW RESUME association. |
| Active recovery versus FIN-pending | A1-minimal `J_I/J_F`; pending corollary `J_P/J_PF` | Active recovery still owes FIN; FIN-pending owes only STOPPED. |
| Active recovery versus terminal | A1-minimal `J_I/J_T`; pending corollary `J_P/J_PT` | Active recovery still owes FIN/STOPPED; terminal forbids later C/R. |
| FIN-pending versus terminal | `J_F/J_T` | First RESUME differs and exactly one side owes STOPPED. |
| Unequal clean residuals | `H()` versus `H(O0)` is the coordinate example | Initial pre-FIN crash separates in SELECTOR/PRIV with zero ordinary C; X separates PUBLIC with one. |

This completes all ten unordered pairs among clean, DOWN-idle, DOWN-pending,
FIN-pending, and terminal phase families. Section 10 listed only idle/pending
and FIN-pending/terminal; its omission of clean/DOWN and active/special phase
responsibilities is why its MUST list fails.

### 4.2 MAY REBUILD / MAY RECOMPUTE — conditional formal results

The following separately materialized forms need not themselves survive if
all named inputs and the identified frozen machinery do:

| Derived material | Reconstruction basis | Verdict boundary |
|---|---|---|
| Residual, allowance, phase, and pending pre/post association | Exact validated cut prefix, initial residual/allowance, and Sections 2-4 fold | MAY REBUILD as separate forms; the underlying behavioral distinctions remain MUST. |
| Serialized RESUME/ALLOWANCE frames | Derived `(r,d)` plus exact templates | MAY REBUILD formally. |
| Viewer projections and trace-set ordering | Complete full suffix family plus projection, codec, dedup, and sort rules | MAY REBUILD formally. |
| D/G domains, order, and encoded table lengths | Frozen cut corpus, transitions, exploration rule, and codec | MAY RECOMPUTE formally; one implementation found 82,224/64,067. |
| Formal outcomes for fixed cuts and policy bytes | Cut, controller, scheduler, specification, and evaluator | MAY RECOMPUTE within FORMAL. |
| Section 8.7 canonical witness | Cut pair, complete policy domains, evaluator, scope eligibility, and byte order | MAY RECOMPUTE mathematically; arbitrary-pair bytes/cost remain UNKNOWN. |

These are conditional derivations, not executed cold-rebuild results. The
availability, identity, trust, and cost of every source input and reconstruction
mechanism remain charged.

### 4.3 MAY FORGET — continuation only

The bounded all-scope quotient permits forgetting:

- exact clean words within one residual class; `H()`/`H(RI)` is the smallest
  distinct merger;
- pending alias or T stage when the directed `(pre,post),d` class is equal;
  interrupted RI/RN and T-before-A/T-after-A are the smallest examples;
- exact recovery-source prefixes that normalize to the same idle or directed
  pending condition;
- source residual, unused d, and exact source history after FIN, respectively
  after terminal completion; and
- manifest retention between service executions when the only contract is
  future continuation and no manifest-backed claim must remain auditable.

PUBLIC projection alone permits more mergers—reversed directed edges,
NONE-versus-equal OLD/NEW projected sets, and all active states at d=0—but
SELECTOR/PRIV still distinguish them. Such PUBLIC-only mergers are not
contractual MAY FORGET verdicts.

### 4.4 Claim evidence is a separate ledger

If the corresponding claim must remain auditable, these responsibilities are
MUST SURVIVE as **claim evidence**:

- the exact prefix for exact-history, cut-membership, crash-ordinal, or witness
  validation claims;
- manifest bytes for the particular manifest-backed claim;
- the captured raw family for a claim about what was observed;
- crossed A evidence for a claim that an attempt was captured before the cut;
- the governing experiment context for a timeout/non-totality claim; and
- the frozen specification identity used to interpret every other carrier.

This does not make them service continuation state. Conversely, deleting them
must change the corresponding claim to UNKNOWN, not to “nothing happened.”
The current kind-1 carrier already loses partial captured prefixes, and the
governing envelope is unbound, so retaining the current manifest bytes is not
sufficient to retain those claims correctly.

## 5. Simultaneous total-system accounting

No weighted score is defined, and strength in one row cannot compensate for a
failure or UNKNOWN in another.

| Dimension | Evidence and burden now visible | Verdict |
|---|---|---|
| Information/distinction preservation | Blind/literal analysis supports 329 FORMAL classes; the executable derives 329 under fixed words but does not prove adaptive probe completeness; exact prefix and branch orientation are distinguished | Formal quotient supported analytically; FAIL for partial-timeout evidence and the incomplete responsibility ledger |
| Persistent state | Behavioral lower bounds and conditional derivations are identified without choosing a shape | No physical carrier, deletion/cold-start run, durability result, or minimum encoding: UNKNOWN |
| Semantic machinery | Exact transition fold, five viewers, all-cut relation, D/G closures, branch generator, codecs, and witness order | Core PASS; raw router and raw-description boundary FAIL/UNDERDEFINED |
| Human cognition | A 49,826-byte candidate spans phase routing, two large policy domains, viewers, evidence precedence, and separate ledgers | No controlled reviewer study; raw/evidence forks already prevent one literal reproduction: UNKNOWN empirically |
| Authoring burden | Twelve bounded requests are simple, but a complete controller has 82,224 entries and external policies/context must be supplied | Exact table burden known; usable authoring method, validation burden, and error rate UNKNOWN |
| Query/navigation burden | QUERY, CURRENT, and X provide bounded value/state access; no search/navigation infrastructure was built | Bounded formal queries PASS; discovery, arbitrary queries, causal/counterfactual explanation, and scaling UNKNOWN/unsupported |
| Runtime | Author run: 287.949434 s recorded elapsed, 175,232 KiB peak RSS; separate run: 294.08 s wall, 174,052 KiB peak RSS | Measurements apply only to the experiment; service, recovery, and canonical-search runtime UNKNOWN |
| Storage | Controller encoding 82,245 bytes; scheduler encoding 8,030 bytes; exact prefixes/manifests are variable | No selected representation, retention volume, write amplification, or physical storage result: UNKNOWN |
| Operations | Formal DOWN/READY and terminal behavior exist | No process restart, power failure, backup/restore, corruption, monitoring, or resource-bound campaign: UNKNOWN |
| Trusted computing base | Candidate names runtime, OS, transport, serializer, compiler, capture, selector, policy providers, canonicalizer, spec/build, envelope signer, and human adjudication | No one-at-a-time perturbation or independent oracle: UNKNOWN |
| Evolution | One bounded E0→E1 residual transition is formalized | Meaning-changing schema/spec evolution, migration, rollback, and cross-version author/apply are UNKNOWN or unsupported |
| Portability | Primitive bytes and finite algorithms are specified | No second implementation, platform, language, media, or context comparison: UNKNOWN |
| Explainability | X explains current `(O,P,G,V)` and formal witnesses can expose a separating future | Viewer-authorized causal/counterfactual explanation and human comprehension are UNKNOWN |
| Information-loss risk | Q7, timeout-prefix, manifest masking, timeout tie, and envelope-context collisions are minimized | Known finite FAIL; physical loss/corruption risk remains UNKNOWN |
| External services | Controller, scheduler, capture, selector, manifest store, spec distribution, canonicalizer, and envelope authority carry responsibilities | Named but never severed; availability, recovery, authenticity, and independence UNKNOWN |
| Physical/unlike realizations | Candidate admits FORMAL only and explicitly makes no media/power/completion claim | REALIZE and cross-realization equivalence UNKNOWN; scope exclusion earns no capability credit |

## 6. Mandatory attacks and where the complexity is now

| Attack | R0.1J result | Where the complexity is now |
|---|---|---|
| DELETE | Deleting clean-versus-DOWN or active/special phase responsibility causes the Q7/phase-matrix collisions. No physical deletion run exists. | In the exact prefix or another phase-bearing carrier, the fold, recovery router, and the mechanism that survives the execution boundary. |
| MERGE | Within-class mergers F1-F9 are permitted; cross-class mergers such as `J_C/J_I`, `J_I/J_F`, and directed-edge reversal are forbidden. Timeout kind 1 performs an evidence merge that FAILS. | In all-scope branch identity, SELECTOR/PRIV labels, phase obligations, and a lossless evidence carrier. |
| DERIVE | Residual, d, phase, frames, and projections have exact formal derivations. | In retained source bytes, specification identity, validation/fold code, and reconstruction work; none is zero complexity. |
| RECOMPUTE | D/G, outcomes, and witness search are finite; D/G were recomputed from fourteen clean coordinates plus 854 normalized recovery seeds. Cold restart and arbitrary-pair witness regeneration were not. | In closure exploration, large policy domains, canonicalizer runtime/memory, and source availability. |
| COLLIDE | FIN/pass directly separates clean from recovery; fixed-word refinement partitions the normalized recovery conditions, while blind/literal analysis supplies the stronger formal quotient support. No executable exhaustive adaptive collision search ran. The timeout carrier and manifest/context classifier have explicit collisions. | In progress-bearing timeout evidence, per-case verdict composition, context binding, and the still-unexecuted adaptive completeness argument. |
| FUTURE | FIN/pass distinguishes clean from recovery with zero ordinary C; later FIN/STOPPED obligations distinguish every phase family. | In the full phase responsibility that Section 10 omitted, not merely in r or d. |
| EXTERNALIZE | Manifest context, controller, scheduler, selector, capture, spec, and canonicalizer are external or externally influenced; no severing test passed. | In service availability, identity binding, signatures, distribution, recovery, and organizational/human procedures. F04 shows a concrete unbound-context failure. |
| REALIZE | FORMAL is the only admitted realization. | All media, process, power-loss, physical completion, rollback, corruption, and cross-realization work remains outside the candidate and UNKNOWN. |
| COGNITION | No bounded human experiment ran; Section 10 and raw/evidence interfaces admit incompatible literal readings. | In what authors/reviewers must infer across sections, how they construct enormous tables, and how they bind claims to evidence/context. |
| TCB | Influences are inventoried but unperturbed; origin-0 agreement deliberately shares the generator and is not independent verification. | In runtime, compiler, codecs, capture, policies, selector, specification, canonicalizer, envelope signer, and adjudicator. |

After every apparent simplification, the answer to “where is the complexity
now?” is concrete:

- removing repeated semantic fields from EncCut moves authority to exact-prefix
  capture, membership validation, fold code, and specification availability;
- replacing cut-kind gates with one relation moves work to a genuinely total
  cross-phase router and common policy identity;
- deriving rather than persisting residuals, phases, projections, or closures
  moves work to surviving inputs, deterministic machinery, cold-start cost,
  and its TCB;
- deduplicating projected branches moves contractual orientation to the full
  family, SELECTOR, and PRIV;
- withdrawing an arbitrary MUST mask avoids a false guarantee but moves any
  such query to evidence design and adjudication, and the capability remains
  unsupported;
- externalizing manifests and experiment envelopes moves retention, binding,
  authority, and availability outside the service; and
- admitting only FORMAL avoids false physical claims but leaves the required
  unlike-realization obligation entirely undone.

## 7. Archive replay disposition

The quarantined archive confirms real formal improvements without converting
them into total-system success:

- SELECT-new is distinct from ordinary reply completion;
- one ungated all-cut relation replaces the prior kind-gated contradiction;
- PUBLIC and privileged separator minima are viewer-correct;
- exact prefix is the sole EncCut authority;
- formal outcomes and completed raw realization evidence are separate;
- D/G closure and witness-scope rules are explicit; and
- manifest evidence is separated from behavioral continuation state.

Those changes relocate responsibility to the prefix validator, router,
controller/scheduler domains, full branch family, selector, capture peer,
canonicalizer, frozen specification, evidence carrier, and experiment
envelope. The archive adds no smaller counterexample than the fresh failures;
it also supplies no missing physical, human, portability, or trust evidence.

## 8. Final feasibility verdict

R0.1J is a valuable bounded formal model. Its history corpus and policy-key
closures are executable, while the fresh blind/literal audits separately
support its ungated continuation relation, normalized quotient, and canonical
rules. The executable derives the same 153/329 counts under 1,885 fixed-word
probes but does not prove that basis complete for adaptive controllers. With
that evidence boundary explicit, R0.1J narrows the behavioral information
lower bound more defensibly than R0.1I.

It does not reach the requested first milestone. The declared total contract
still loses a captured timeout prefix, permits UNKNOWN to mask a finite FAIL,
does not bind per-case timeout evidence to governing context, does not define
one verdict for identical manifest bytes, and does not list every
future-forced phase responsibility. Its raw operation/router surface is also
not total. The corrected ledger can state what the formal quotient forces,
but no deletion/rebuild campaign establishes where or how that information
survives between physical executions.

Accordingly:

- **MUST SURVIVE** is presently a formal information-responsibility lower
  bound, plus a separate claim-evidence ledger;
- **MAY REBUILD** is conditional on named inputs and machinery whose survival
  is unproved;
- **MAY FORGET** is valid only inside the stated continuation class and scope;
  and
- external service survival, human feasibility, TCB closure, operational
  persistence, portability, and unlike physical realization remain UNKNOWN.

No representation, primitive, layer, constructor, storage mechanism, or
architecture is selected by this audit. A corrected future candidate would
still need fresh collision attacks and at least two materially unlike physical
realizations before any total-system or persistence claim could pass.
