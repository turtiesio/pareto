# ZERO GROUND R0.1I — Quarantined Archive Replay

## 0. Scope, integrity, and quarantine

The fresh R0.1I break was frozen before this replay. Prior R0.1H material was then admitted only as an adversarial vocabulary. No prior representation, proposed realization, repair, architecture, or ontology is adopted here.

Read authorities and verified SHA-256 values:

| Input | SHA-256 | Role |
|---|---|---|
| `ARCHIVE-REPLAY-R01H.md` | `815959501ae4dd06882abf565ea4c8536293f9e3803246894479e6b12b20a2c0` | Consolidated old attack inventory |
| `MATH-AUDIT-R01H.md` | `4be36240e23804b0dac6c20c62b14a07f96e8b0ff3c456af0baf02e34d9515be` | Old finite counterexamples and arithmetic |
| `POSTFREEZE-BREAK-R01H.md` | `c97e6ef26fbdc0084c739cf64518fd51ff1668ec8b6eb4e1e5e20cebf99d669c` | Old fresh break |
| `FEASIBILITY-AUDIT-R01H.md` | `37f5ab50218e2262e5d9aa0b19f4eca7559c02752d7164facef9b80487baa0de` | Old aggregate feasibility attacks |
| `HISTORY-SEED-R01I.md` | `d52c083c34aa37144fa47ee8a02d9279adb75029613125f38dad5a98b0df8d9c` | Frozen R0.1I candidate |
| `POSTFREEZE-BREAK-R01I.md` | `57f007f0457595e5545eb9dd76fda210b9eea94483a1d74cb6d074230b8ac015` | Frozen fresh R0.1I break |

Verdicts are strict:

- **PASS** means the specific old counterexample is discharged by the frozen R0.1I mathematical boundary without an extra choice.
- **FAIL** means a smallest finite R0.1I counterexample remains or replaces it.
- **UNKNOWN** means the required capability, external severing, realization, human evidence, or TCB evidence is absent.

An unsupported capability is UNKNOWN, not a capability PASS. A semantic PASS can coexist with **MOVED** complexity when another named participant must now carry the responsibility.

## 1. Replay result in one view

R0.1I genuinely fixes these R0.1H defects at the abstract boundary:

- SELECT-new is now a semantic application, so interrupted changing requests have a single full-history residual;
- OLD/NEW branch identity survives in the privileged family before public deduplication;
- the client controller is explicitly CLIENT-only and cannot observe A or F;
- one common controller/scheduler truth-table grammar covers adaptive paths;
- FIN-pending and terminal recovery have exact, different suffixes;
- both ATTEMPT gaps receive selector coverage, giving the corrected `3n+2t+4` count;
- a fixed Must carrier and an explicit vacuous-truth rule replace the old missing semantics.

Those fixes move substantial responsibility to the selector, F-bearing full history, branch-family manifest, controller, scheduler, canonicalizer, capture peer, and exact specification. None of that movement is deletion or realization evidence.

The replay also confirms the frozen R0.1I failures:

- same-cut-kind equivalence implies 415 contractual recovery classes, not 315;
- a pre-FIN idle crash is a zero-ordinary-message SELECTOR/Priv clean separator;
- EncCut, EncBranchRecord, scheduler closure, and witness-viewer admissibility do not fix one canonical byte string;
- vacuous Must truth is not distinguished from non-vacuous operational evidence in the truth mask.

No archive-only failure is smaller than the already-frozen R0.1I break.

## 2. Direct replay of the old fresh-break counterexamples

| Old R0.1H counterexample | Smallest R0.1I replay witness | Verdict | Fixed versus moved disposition |
|---|---|---|---|
| Completed-prefix O/P/G conflicts with SELECT-new after interrupted `AI` | `H()`; C:AI; crash before R; compare F:SELECT old/new and their RESUME values; retry AI or issue X | **PASS** | **Fixed semantically. MOVED:** selected residual and branch association now live in F/Priv and the manifest/evaluator. No selector/manifest realization exists. |
| Same conflict for observation | `H()`; C:O0; crash before R; OLD resumes U, NEW resumes 0; X | **PASS** | **Fixed semantically. MOVED** to the full application fold and selector evidence. |
| Same conflict for evolution | `H()`; C:E; crash before R; OLD resumes E0, NEW resumes E1; retry E or issue K/X | **PASS** | **Fixed semantically. MOVED** to the full fold, selector, and exact specification. |
| Equal public prefix was incorrectly treated as equal full history | The interrupted AI OLD/NEW pair has equal PUBLIC recovery projection but different F frames and Priv records | **PASS** | **Fixed:** full exact history and viewer projection are distinct contractual notions. **MOVED:** full-history evidence requires the privileged manifest. |
| Adaptive schedules lacked one common identity | Compare `H()` with `H(AI)`; controller sends X and sends T only on `P=EMPTY`; apply one total G-vector | **PASS** | **Fixed at the semantic level** by total D/G tables. **MOVED:** closure, encoding, and availability are charged to controller/scheduler/canonicalizer; exact G bytes still fail canonical prediction 13. |
| Controller might or might not observe A | Crash after T's A and before R; run the same CLIENT-only retry table | **PASS** | **Fixed:** controller sees CLIENT only, capture sees A only, scheduler sees PUBLIC. **MOVED:** capture availability remains external. |
| Must had no hidden-branch carrier | Interrupted AI, both ordered OLD/NEW records, public dedup, ten-bit family verdict | **PASS** | **Fixed semantically** by branch records before projection. **MOVED:** branch-family retention and truth evaluation are manifest/canonicalizer responsibilities. |
| Must vacuity/inapplicability was unspecified | Compare a no-crash family with a lifecycle-correct crash family | **PASS for the old ambiguity** | R0.1I explicitly chooses vacuous truth and no inapplicable value. The newer B14 evidence attack still **FAILS** because the encoded truth mask does not distinguish vacuous from non-vacuous support. |
| Crash after STOPPED had two possible complete suffixes | `H()`; FIN; STOPPED; terminal-gap crash; F:RESUME TERMINAL; READY | **PASS** | **Fixed:** exact terminal suffix is frozen and READY does not reopen C/R. Lifecycle realization remains UNKNOWN. |
| Canonical witness order lacked complete bytes | Encode the interrupted AI/D orientation pair in two independent readings | **FAIL** | Many primitive encodings were added, but optional EncCut/EncBranchRecord presence, G scope, and viewer eligibility remain underdetermined. Complexity was moved to an incomplete canonicalizer. |
| Padded selector count omitted T's second gap | One-message word T; cover C→A and A→R with OLD and NEW | **PASS** | **Fixed:** nine slots for T and totals `24,906` / `3,910,242` are now frozen. |
| Total between-crossing quotient was absent | Enumerate 854 conditions and apply literal same-cut-kind equivalence | **FAIL** | Inventory is now finite, but the claimed 315 quotient merges PENDING_NON_T, T_PRE_A, and T_POST_A despite the same-kind restriction. Literal result is 415. |

## 3. Full archive attack crosswalk

Duplicate statements across the four R0.1H reports are consolidated once below; every distinct attack row from the old archive replay is represented.

| Old attack | Smallest R0.1I witness or probe | Verdict | Disposition |
|---|---|---|---|
| Malformed framing | Empty bytes, LF alone, or `QUERY` without LF | **PASS** | Fixed abstract classifier returns typed UNSUPPORTED by priority. Atomic parser/transport realization remains UNKNOWN. |
| Empty input versus FIN | Empty frame versus typed C:FIN | **PASS** | The distinction is exact. This is not support for empty-message behavior. |
| Two simultaneous unsupported causes | One description with noncanonical encoding and unknown viewer | **PASS for reason priority** | `UNSUPPORTED(ENCODING)` precedes viewer failure. The separate canonical-encoding ambiguity still fails B00/B23. |
| FIN output-commit cut | `H()`; FIN; crash before STOPPED | **PASS** | Exact FIN_PENDING recovery owes one STOPPED. Physical atomicity/durability remain UNKNOWN. |
| Post-terminal crash | `H()`; FIN; STOPPED; crash at END | **PASS** | Exact terminal recovery emits no C/R and closes with READY. |
| Author crash/retry selector collision | Interrupted AI from EMPTY, then retry | **PASS** | SELECT old/new now determine old/new residuals without inventing the interrupted R. Selector evidence is externalized. |
| Observation/evolution recovery identity | Interrupted O0 then X; interrupted E then E/K/X | **PASS** | Full fold processes SELECT-new as application. |
| Selector completeness and trace binding | Force both interrupted-AI branches and bind each to its exact suffix | **UNKNOWN** | Branch-family semantics is present; no selector, manifest writer, subject binding, or run evidence exists. **MOVED**, not fixed empirically. |
| Nondeterministic May/Must correlation | Interrupted AI, recover, then FIN so public endpoints may dedup | **PASS at model level** | Priv and Must are computed before viewer dedup. Manifest/canonicalizer realization is UNKNOWN; B14 vacuity evidence separately fails. |
| Adaptive scheduler common domain | X-then-conditional-T controller on `H()` versus `H(AI)` | **PASS at model level** | Common total vector fixes the old path-domain ambiguity. G generation/canonical bytes and runtime are still UNKNOWN/failed. |
| Action crash before/after A | From `H(O1,AI)`, crash before A or after A; retry T | **PASS at crossing model** | Full histories contain one versus two A crossings. Receiver effects remain UNKNOWN. |
| Controller visibility of A | Same post-A crash; controller decision conditioned only on CLIENT | **PASS** | Visibility is now explicit. Capture-to-client communication is forbidden during a run. |
| Exactly-once receiver effect | Sever beyond A and ask whether the receiver applied/deduplicated | **UNKNOWN** | Receiver effects are excluded and unevidenced. Exclusion is not capability evidence. |
| Clean stale evolution identity | `H()` versus `H(E)`; issue K, X, T, or E | **PASS** | E0/E1 is exposed and repeated evolution is distinct. Interrupted evolution is also semantically repaired. |
| External specification selector | Same boundary history under two external rule bundles | **UNKNOWN** | Exact seed bytes are authoritative, but bundle/manifest selection, availability, and identity have no implementation or TCB evidence. **MOVED** to specification/build inputs. |
| Rejection provenance and lineage | `H(AI)` versus `H(AI,RI)`; issue X | **UNKNOWN** | R0.1I exposes current residual, not lineage or prior rejection provenance. B02 identifies this explanation gap; scope exclusion is not a capability pass. |
| Hidden path/cache influence | `H()` versus `H(RI)`; compare all legal futures in two realized paths | **UNKNOWN** | Finite model congruence passes, but no runtime/cache/path realization was forced. Behavioral equality does not prove physical absence. |
| Corrupt/counterfeit/rollback image | One deletion, substitution, counterfeit, or rollback followed by recovery | **UNKNOWN** | Such images are excluded and no robustness experiment exists. |
| Realization import validation | Present one invalid or replayed recovery image | **UNKNOWN** | No realization image grammar/importer exists. |
| Externalized capture peer | `H(O1,AI)`; sever capture peer; issue T | **UNKNOWN** | A remains a named external responsibility. No availability, durability, or authenticity trial ran. **MOVED**, not deleted. |
| Externalized continuation token | Lose/substitute external controller authority and issue a request | **UNKNOWN** | Authentication/authority is excluded. No conforming token experiment exists. |
| Exact-history interpreter request | Add an equality-to-exact-history operation and compare `H()`/`H(RI)` | **UNKNOWN** | Arbitrary interpreter/query installation is excluded; this does not add capability evidence or refute the bounded alphabet. |
| Injectivity of exact-history encoding | Encode the smallest distinct pair `H()`/`H(RI)` | **FAIL** | R0.1I claims exact canonical bytes, but EncCut presence is not normatively mapped, so independent encodings can differ. |
| One-message separator length | `H()` versus `H(O0)`; controller chooses FIN; crash before FIN | **FAIL contractually** | Zero ordinary messages expose different F:RESUME values to SELECTOR/Priv. X remains the minimal PUBLIC separator only. |
| Canonical witness serialization | AI EMPTY→ID versus D ID→EMPTY; equal PUBLIC endpoints, privileged orientation differs | **FAIL** | Viewer-code eligibility and optional canonical components are not fixed. CLIENT code 0 and SELECTOR code 3 are both text-consistent. |
| Clean quotient arithmetic | Fold all 157 words | **PASS** | Fourteen coordinates, stated multiset, 2,351 same and 9,895 unequal pairs reproduce. |
| Same-class path independence | `H()` versus `H(RI)` under all finite modeled futures | **PASS at model level** | Same-coordinate congruence is now finitely specified. Hidden implementation influence remains UNKNOWN; responsibility moved to controller/scheduler/specification. |
| Total between-crossing classification | Shortest cross-kind merger: interrupted RI versus T-before-A from `H()`, both at `r=(U,EMPTY,E0),d=2` | **FAIL** | Printed 315 treats their suffix behavior as one class while same-cut-kind equivalence forbids comparison. |
| DERIVE exact replies/action bytes | Remove a target form and reconstruct from surviving semantics/spec | **UNKNOWN** | Mathematical derivability is stated; no target was removed or cold-rebuilt and no cost was measured. **MOVED** to specification/serializer. |
| RECOMPUTE on recovery | Restart without residual/allowance/branch outcomes and reconstruct | **UNKNOWN** | No reconstruction machinery was executed. Controller, manifest, and canonicalizer remain charged. |
| Linear schedule arithmetic | One-message T and all words length 0..3 | **PASS** | `18,965` and `2,977,505` reproduce. They are structures, not run counts. |
| Padded selector arithmetic | T has both C→A and A→R barriers | **PASS** | R0.1I freezes the corrected `3n+2t+4`, `24,906`, and `3,910,242`. |
| Unlike physical realization | Two unlike families, one physical fault boundary, recovery, then X/K/T | **UNKNOWN** | No family or independent physical oracle exists. |
| Evidence-versus-behavior inference | Ask whether exact mathematical traces prove storage, security, or receiver effects | **PASS for non-inference** | R0.1I explicitly reports those facts UNKNOWN. This is not evidence for the facts themselves. |
| Evidence dossier/subject binding | Request build identity, manifest, branch association, raw capture, and replay | **UNKNOWN** | No dossier or writer exists. |
| TCB mutation | Perturb parser through build/specification inputs one at a time | **UNKNOWN** | Nothing was perturbed. |
| Independent specification reproduction | Give two fresh implementers only R0.1I and compare canonical witnesses/quotient | **FAIL** | The 315/415 contradiction and canonical optional/viewer choices permit different lawful results. |
| Human cognition measurement | Measure time, choices, recall, and errors | **UNKNOWN** | No controlled trial or resource bound exists. Specification reproducibility already fails independently. |
| Privacy, authority, concurrency, clocks, resources | Admit the smallest excluded case and repeat a query/action | **UNKNOWN** | These capabilities remain excluded and untested. |

## 4. Replay of old recovery-quotient and feasibility probes

| Old open probe | Smallest R0.1I replay | Verdict | Disposition |
|---|---|---|---|
| Recovery-cut placement | Crash, append DOWN, stop before SELECT/RESUME | **PASS** | Exact cut is now after DOWN and before recovery choice. |
| Inherited remaining depth | Interrupt the first ordinary C from d=3 | **PASS** | C atomically changes d to 2; F:ALLOWANCE must agree. External controller/manifest survival remains UNKNOWN. |
| Selector at idle/clean gap | Crash before the next proposed crossing | **PASS** | SELECT is forbidden at idle; RESUME ACTIVE is exact. |
| Public versus label-preserving recovery | AI EMPTY→ID versus D ID→EMPTY | **PASS for projection distinction** | PUBLIC dedups unordered endpoints; Priv/SELECTOR retains direction. **MOVED** to privileged evidence. |
| FIN-pending versus terminal | Compare their recovery suffixes | **PASS** | FIN-pending owes STOPPED; terminal owes none. Zero ordinary messages separate. |
| T before A versus T after A continuation | Crash at C→A versus A→R at fixed r,d | **FAIL under literal equivalence** | Future service suffixes match, but cut kinds 3 and 4 cannot merge under the same-kind rule. The crossed-A full-history fact remains distinct either way. |
| Recovery-condition inventory | Exhaustive fold over all clean histories and future lengths | **PASS** | 854 conditions and all exact phase totals reproduce. |
| Contractual recovery quotient | Apply candidate equivalence to those 854 conditions | **FAIL** | Literal result is 415 classes; printed 315 requires unstated cross-kind merging. |
| Zero-depth active collapse | Consume all three ordinary C occurrences, recover, then forced FIN | **PASS publicly** | PUBLIC active conditions collapse at d=0; SELECTOR/Priv selected residual remains contractual, and same-kind restriction still partitions cut kinds. |
| Complete branch truth carrier | One interrupted no-op with OLD and NEW equal residuals | **PASS semantically** | Both label records remain even when projected traces dedup. Manifest/canonicalizer realization remains UNKNOWN. |

## 5. Where complexity moved

| Responsibility attacked in R0.1H | R0.1I carrier of that responsibility | Classification |
|---|---|---|
| Selected residual/application after interrupted request | F:SELECT, full application fold, F:RESUME, Priv branch family | **MOVED; semantic defect fixed, realization UNKNOWN** |
| Old/new correlation hidden by public projection | Ordered Priv before May dedup | **MOVED; semantic carrier fixed, manifest evidence UNKNOWN** |
| Adaptive continuation choice | Total controller table over D | **MOVED; finite model PASS, controller availability/recovery UNKNOWN** |
| Adaptive crash choice | Total scheduler bit vector over G | **MOVED; semantic identity PASS, canonical G/TCB UNKNOWN or failed** |
| Remaining depth | Client/controller derivation plus manifest and F:ALLOWANCE | **MOVED; arithmetic PASS, severing UNKNOWN** |
| Captured A occurrence | Independent capture peer and full history | **MOVED; abstract crossing PASS, peer durability/authenticity UNKNOWN** |
| Exact full-history versus viewer evidence | Full F-bearing history, four projections, privileged manifest | **MOVED; projection semantics PASS, availability UNKNOWN** |
| May/Must computation | Complete branch family plus canonicalizer | **MOVED; carrier fixed, vacuity-evidence and canonical bytes FAIL** |
| Exact reply/action/recovery bytes | Frozen specification plus serializer/interpreter | **MOVED; mathematical rebuild plausible, DERIVE/RECOMPUTE UNKNOWN** |
| Specification identity | Exact seed bytes and future manifest/build input | **MOVED; bundle selection and TCB UNKNOWN** |
| Human reconciliation | Fresh implementer/canonicalizer choices | **NOT DISCHARGED; specification reproduction FAIL, measured burden UNKNOWN** |

No row above establishes that responsibility vanished. It only identifies where R0.1I says the behavior must now come from.

## 6. Required attack battery after archive replay

| Attack | R0.1I replay verdict |
|---|---|
| DELETE | **UNKNOWN** — no responsibility was removed and cold-started. |
| MERGE | **FAIL** — the printed 315 quotient performs 100 cross-kind class mergers forbidden by the equivalence rule. |
| DERIVE | **UNKNOWN** — derivability prose is not a deletion/rebuild experiment. |
| RECOMPUTE | **UNKNOWN** — controller/manifest/canonicalizer reconstruction was not run. |
| COLLIDE | **FAIL** — canonical bytes and witness viewer identity admit text-consistent collisions/readings. |
| FUTURE | **FAIL** — pre-FIN crash gives a zero-C privileged separator contrary to prediction 3; literal recovery futures give 415 classes. |
| EXTERNALIZE | **UNKNOWN** — controller, peer, selector, manifest, and specification were named but not severed. |
| REALIZE | **UNKNOWN** — no unlike implementation or physical family exists. |
| COGNITION | **FAIL for exact specification reproduction; UNKNOWN for measured humans**. |
| TCB | **UNKNOWN** — the inventory is unperturbed. |

## 7. Final archive disposition

Archive replay changes no fresh R0.1I verdict. It shows that R0.1I repaired several real R0.1H semantic defects, but repaired them by assigning exact new obligations to full-history F evidence, branch families, controller/scheduler tables, manifests, capture, canonicalization, and the specification.

The repaired finite pieces are valuable: selected application, fixed viewer visibility, exact terminal recovery, corrected selector padding, clean arithmetic, branch preservation, and a common adaptive-policy grammar. They do not offset the frozen 315/415 contradiction, zero-message privileged separator, or canonical-byte ambiguity.

The physical, operational, externalization, human, and TCB archive attacks remain UNKNOWN. Unsupported space receives no capability credit. One finite FAIL continues to dominate the model-level passes.
